<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://dhruveshp.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://dhruveshp.com/" rel="alternate" type="text/html" /><updated>2026-08-30T21:24:17+00:00</updated><id>https://dhruveshp.com/feed.xml</id><title type="html">Dhruvesh Patel</title><subtitle>Personal academic homepage of Dhruvesh Patel, PhD researcher at the Information Extraction and Synthesis Laboratory (IESL), UMass Amherst.</subtitle><author><name>Dhruvesh Patel</name><email>dhruveshpate@umass.edu</email></author><entry><title type="html">Discrete Diffusion Language Models, Explained</title><link href="https://dhruveshp.com/2026/08/21/discrete-diffusion-language-models-explained/" rel="alternate" type="text/html" title="Discrete Diffusion Language Models, Explained" /><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://dhruveshp.com/2026/08/21/discrete-diffusion-language-models-explained</id><content type="html" xml:base="https://dhruveshp.com/2026/08/21/discrete-diffusion-language-models-explained/"><![CDATA[<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Autoregressive</th>
      <th>Discrete diffusion</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Decoding order</td>
      <td>Left-to-right, fixed</td>
      <td>Parallel, no fixed order</td>
    </tr>
    <tr>
      <td>Steps at inference</td>
      <td>≈ one per output token</td>
      <td>T, set independently of length</td>
    </tr>
    <tr>
      <td>Attention pattern</td>
      <td>Causal</td>
      <td>Bidirectional</td>
    </tr>
    <tr>
      <td>Infilling a fixed template</td>
      <td>Needs special training</td>
      <td>Native to the objective</td>
    </tr>
    <tr>
      <td>Exact likelihood</td>
      <td>Exact, via the chain rule</td>
      <td>A variational bound</td>
    </tr>
  </tbody>
</table>

<p><em>Table 1 — Two decoding paradigms at a glance.</em></p>

<h2 id="1-two-ways-to-write-a-sentence">1. Two ways to write a sentence</h2>

<p>A standard language model writes left to right. At each step it looks at everything written so far and predicts the next token; string enough predictions together and you have a paragraph. This is autoregressive generation, and it is the basis of essentially every widely deployed chat model today.</p>

<p>It has a structural consequence: generation is sequential.<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup> Producing N tokens takes roughly N forward passes, and the model can only condition on what comes before a position, never after. Editing the middle of a passage, or filling a blank surrounded by fixed text on both sides, doesn’t fit the left-to-right story naturally.</p>

<p>Diffusion models, dominant in image generation, offer a different shape. They start from pure noise and repeatedly denoise it, refining the whole canvas at once rather than piece by piece. Applying the same idea to text means defining what “noise” means for a sequence of discrete tokens — and that turns out to be the interesting part.</p>

<h2 id="2-adding-noise-to-tokens">2. Adding noise to tokens</h2>

<p>Images are continuous — a pixel value can be nudged by a small amount, so Gaussian noise makes sense. Tokens are discrete: there’s no “slightly wrong” word, only the right one or a different one entirely. So discrete diffusion models define corruption differently. The formulation used by nearly every discrete diffusion language model in practice is absorbing-state, or masking, diffusion — as the corruption level <em>t</em> increases from 0 to 1, each token independently has a chance of being replaced by a special <code class="language-plaintext highlighter-rouge">[MASK]</code> token, and once masked it stays masked until the reverse process restores it.<sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup> At <em>t</em> = 1, the entire sequence is <code class="language-plaintext highlighter-rouge">[MASK]</code>; at <em>t</em> = 0, it’s the untouched original.</p>

\[q(x_t^i \mid x_0^i) = (1 - t) \cdot \mathbb{1}[x_t^i = x_0^i] + t \cdot \mathbb{1}[x_t^i = \texttt{MASK}]\]

<p>Each position independently keeps its original token with probability $1-t$, or becomes <code class="language-plaintext highlighter-rouge">MASK</code> with probability $t$.</p>

<p>This absorbing-state formulation is a special case of the general discrete diffusion framework <a href="#references">[1]</a>, and recent theoretical work shows it implicitly models many conditional distributions of the clean data at once <a href="#references">[4]</a>.</p>

<div class="widget-box widget-mask-demo" data-words="the model learns to predict masked tokens">
  <div class="widget-box__label">Try it — masking schedule</div>
  <div class="widget-box__tokens"></div>
  <input type="range" min="0" max="100" step="1" value="40" aria-label="Corruption level t" />
  <div class="widget-box__scale">
    <span>t = 0</span>
    <span data-scale-t="">t = 40%</span>
    <span>t = 100</span>
  </div>
  <svg viewBox="0 0 280 120" width="100%" height="130">
    <path data-step-path="" fill="none" stroke="var(--bs-ink)" stroke-width="1.5" opacity="0.4"></path>
    <line data-marker-line="" x1="0" y1="8" x2="0" y2="98" stroke="var(--bs-magenta-500)" stroke-width="1" stroke-dasharray="2,3"></line>
    <circle data-marker="" cx="0" cy="0" r="4" fill="var(--bs-magenta-500)"></circle>
    <text x="2" y="10" font-size="9.5" fill="currentColor" opacity="0.55">fraction masked</text>
    <text x="200" y="112" font-size="9.5" fill="currentColor" opacity="0.55">corruption t &rarr;</text>
  </svg>
  <p class="text-muted" style="font-size: 0.78125rem; margin: var(--bs-space-3) 0 0;">A simplified, word-level stand-in for what happens to sub-word tokens during training.</p>
</div>

<h2 id="3-learning-to-reverse-it">3. Learning to reverse it</h2>

<p>Training a discrete diffusion model means teaching a network to undo this corruption: given a partially masked sequence at some level <em>t</em>, predict the original tokens underneath the masks. Unlike an autoregressive model, which can only attend to earlier positions, this network sees the entire corrupted sequence at once and predicts every masked position in parallel, conditioning freely on tokens before and after it.</p>

<p>That objective looks a lot like masked language modeling — the pretraining task behind BERT.<sup id="fnref:a"><a href="#fn:a" class="footnote" rel="footnote" role="doc-noteref">3</a></sup> The diffusion framing adds a principled loss derived from a variational bound on the data likelihood, which reweights the masked-token cross-entropy by how corrupted the input was <a href="#references">[2]</a>:</p>

\[\mathcal{L} = \mathbb{E}_t \left[ \frac{1}{t} \sum_{i \,\in\, \text{masked}} -\log p_\theta(x_0^i \mid x_t) \right]\]

<p>A cross-entropy loss over the masked positions only, upweighted at low corruption and downweighted near $t=1$.</p>

<h2 id="4-trading-steps-for-quality">4. Trading steps for quality</h2>

<p>Generation starts from a sequence that’s entirely <code class="language-plaintext highlighter-rouge">[MASK]</code> and runs the reverse process for T steps,<sup id="fnref:3"><a href="#fn:3" class="footnote" rel="footnote" role="doc-noteref">4</a></sup> each one filling in — or firming up — a subset of positions based on the model’s predictions, until nothing is left masked. Fewer steps means faster generation but coarser output; more steps means slower generation that tracks the model’s real distribution more closely.</p>

<p>This is a genuinely different failure mode than autoregressive decoding. An autoregressive model can’t change its mind about a token once it’s written; a diffusion model can revise a low-confidence guess in a later step, but it can also lock in a wrong token early and never revisit it if the sampler commits too aggressively.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x = [MASK] * length            # start fully masked
for t in linspace(1.0, 0.0, steps):
    p = model(x)                # predict a distribution for every position
    x = remask(x, p, t)         # commit confident predictions, leave
                                 # the rest masked for the next round
return x
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">steps</code> is chosen at inference time — independent of sequence length.</p>

<h2 id="5-why-bother">5. Why bother</h2>

<p>Three things fall out of this shape almost for free. Infilling — generating into a fixed template, or fixing the middle of a passage without rewriting everything after it — is native to a model that never assumed a left-to-right order, rather than a special case bolted on. Parallel decoding means wall-clock generation time can, in principle, decouple from sequence length, since many positions update per step instead of one token per forward pass. And reusing the same diffusion machinery already proven on images and audio moves language modeling closer to a shared framework across modalities, instead of a separate autoregressive special case.</p>

<h2 id="6-whats-still-unsettled">6. What’s still unsettled</h2>

<p>None of this is a settled replacement for autoregressive modeling yet. Early discrete diffusion language models trailed well-tuned autoregressive baselines by a wide margin; that gap has narrowed substantially at scale — an 8-billion-parameter diffusion model trained from scratch has been shown to perform comparably to a same-sized autoregressive baseline on general, math, and code benchmarks <a href="#references">[3]</a> — but it isn’t fully closed. Inference now carries an extra knob, the number of sampling steps and the schedule for unmasking, that autoregressive decoding doesn’t need. And tooling built around next-token prediction — constrained decoding, KV-caching, most serving infrastructure — doesn’t transfer directly to a model that predicts every position at once.</p>

<h3 id="references">References</h3>

<ol>
  <li>Austin, J., Johnson, D. D., Ho, J., Tarlow, D., &amp; van den Berg, R. (2021). Structured Denoising Diffusion Models in Discrete State-Spaces. <em>Advances in Neural Information Processing Systems</em>, 34, 17981–17993.</li>
  <li>Sahoo, S. S., Arriola, M., Schiff, Y., Gokaslan, A., Marroquin, E., Chiu, J. T., Rush, A. M., &amp; Kuleshov, V. (2024). Simple and Effective Masked Diffusion Language Models. <em>Advances in Neural Information Processing Systems</em>.</li>
  <li>Nie, S., Zhu, F., You, Z., Zhang, X., Ou, J., Hu, J., Zhou, J., Lin, Y., Wen, J.-R., &amp; Li, C. (2025). Large Language Diffusion Models. <em>International Conference on Machine Learning</em>. arXiv:2502.09992.</li>
  <li>Ou, J., Nie, S., Xue, K., Zhu, F., Sun, J., Li, Z., &amp; Li, C. (2025). Your Absorbing Discrete Diffusion Secretly Models the Conditional Distributions of Clean Data. <em>International Conference on Learning Representations</em>.</li>
</ol>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>Speculative decoding drafts several tokens with a small model and verifies them with the large one — it speeds up autoregressive decoding, but doesn’t remove the sequential dependency. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2">
      <p>An “absorbing” state, in Markov-chain terms, is one you can enter but never leave on your own — the chain exits it only when the reverse process intervenes. <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:a">
      <p>BERT masks a fixed ~15% of tokens once; diffusion training samples t uniformly and can mask anywhere from none to all of them, so the model learns to denoise at every corruption level, not just one. <a href="#fnref:a" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:3">
      <p>T is unrelated to sequence length — a 40-token passage and a 400-token one can both be generated in, say, 32 steps; more steps buys quality, not more tokens. <a href="#fnref:3" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name>Dhruvesh Patel</name><email>dhruveshpate@umass.edu</email></author><category term="generative-models" /><category term="language-models" /><summary type="html"><![CDATA[Property Autoregressive Discrete diffusion Decoding order Left-to-right, fixed Parallel, no fixed order Steps at inference ≈ one per output token T, set independently of length Attention pattern Causal Bidirectional Infilling a fixed template Needs special training Native to the objective Exact likelihood Exact, via the chain rule A variational bound]]></summary></entry><entry><title type="html">Continuous-Time Markov Chains: An Intuitive Guide</title><link href="https://dhruveshp.com/2026/05/10/continuous-time-markov-chains/" rel="alternate" type="text/html" title="Continuous-Time Markov Chains: An Intuitive Guide" /><published>2026-05-10T00:00:00+00:00</published><updated>2026-05-10T00:00:00+00:00</updated><id>https://dhruveshp.com/2026/05/10/continuous-time-markov-chains</id><content type="html" xml:base="https://dhruveshp.com/2026/05/10/continuous-time-markov-chains/"><![CDATA[<h2 id="overview">Overview</h2>

<p>In this post, we will go over the basics of continuous-time Markov chains (CTMCs).
The main goal is to provide an intuitive view of CTMCs.
There are some proofs but they don’t use any measure theory—just basic calculus.</p>

<details class="callout callout--note">
  <summary class="callout__label">Prerequisites</summary>

  <p>This post assumes a basic understanding of:</p>

  <ul>
    <li>probability (random variables, expectation, conditional probability)</li>
    <li>calculus (limits, derivatives, integrals)</li>
    <li>discrete-time Markov chains (transition matrices, stationary distributions)</li>
  </ul>
</details>

<h2 id="poisson-processes">Poisson Processes</h2>

<p>The Poisson process is the simplest CTMC, and provides most of the intuition needed for the general case.</p>

<h3 id="counting-process-definition">Counting process definition</h3>

<div class="callout">
  <div class="callout__label">Definition &mdash; Counting process</div>

  <p>A counting process $C_t$ is a <a href="https://en.wikipedia.org/wiki/Stochastic_process">stochastic process</a> that:</p>

  <ol>
    <li>takes values in the non-negative integers, i.e., $C_t \in \mathbb{Z}_{\geq 0}$;</li>
    <li>is non-decreasing: $C_t \geq C_{t^{\prime}}$ for $t \geq t^{\prime}$.</li>
  </ol>
</div>

<p>A counting process is said to have <strong>independent increments</strong> if the number of events occurring in disjoint time intervals are independent.
That is, $C(t+s) - C(t)$ is independent of $C(t)$: the number of events between $t$ and $t+s$ is independent of the number of events between time $0$ and $t$, and vice versa.</p>

<p>A counting process is said to have <strong>stationary increments</strong> (also called time-homogeneous or simply homogeneous) if increments depend only on the length of the time interval and not on its position.
That is, for all $t_1, t_2, s &gt; 0$ and $n \geq 0$,</p>

\[\operatorname{Prob}\bigl(C(t_1+s) - C(t_1) = n\bigr) = \operatorname{Prob}\bigl(C(t_2+s) - C(t_2) = n\bigr).\]

<p>Together, independent and stationary increments mean that increments on disjoint intervals are IID.</p>

<h3 id="poisson-process">Poisson process</h3>

<div class="callout">
  <div class="callout__label">Definition &mdash; Poisson Process</div>

  <p>A counting process $N(t)$ is a Poisson process with rate $\lambda$ if <a href="#references">[1]</a>:</p>

  <ol>
    <li>$N(0) = 0$, i.e., $\operatorname{Prob}(N(0) = 0) = 1$;</li>
    <li>it has independent and stationary increments;</li>
    <li>for small $t$, $\operatorname{Prob}(N(t) = 1) = \lambda t + o(t)$;</li>
    <li>for small $t$, $\operatorname{Prob}(N(t) \geq 2) = o(t)$,</li>
  </ol>
</div>

<p>where $o(t) = { f \in \mathbb{R}^{\mathbb{R}} \mid \lim_{t \to 0} f(t)/t = 0 }$, and in (3) and (4) the notation $o(t)$ means any function from this set.</p>

<p>Therefore,</p>

\[\operatorname{Prob}(N(t) = 1) = \lambda t + o(t) \implies \lim_{t \to 0} \frac{\operatorname{Prob}(N(t) = 1)}{t} = \lambda.\]

<p>One can also show that the following definition is equivalent.</p>

<div class="callout">
  <div class="callout__label">Definition &mdash; Poisson Process (2)</div>

  <p>A counting process ${N(t), t &gt; 0}$ is said to be a <strong>Poisson process</strong> with rate $\lambda$ if <a href="#references">[1]</a>:</p>

  <ol>
    <li>$N(0) = 0$ a.s., i.e., $\operatorname{Prob}(N(0) = 0) = 1$;</li>
    <li>it has independent increments;</li>
    <li>$\operatorname{Prob}(N(s+t) - N(s) = n) = e^{-\lambda t}\frac{(\lambda t)^n}{n!}$ for all $s, t &gt; 0$.</li>
  </ol>
</div>

<p>That is, the number of events in an interval of length $t$ has a Poisson distribution with rate $\lambda t$.
Note that (3) implies stationary increments.
To get some intuition, we show that the first definition implies the second.</p>

<details class="callout callout--proof">
  <summary class="callout__label">Proof</summary>

  <p>Call the two definitions D1 and D2.
We will show that D1(2, 3, 4) implies D2(3).
Let $T_1$ be the first arrival time and denote $p_t(k):=\operatorname{Prob}(N(t)=k)$. Then D1(3) implies:
\(\lim_{t \to 0} \frac{p_t(1)}{t} = \lambda. \qquad (3)\)
From D1(1) we have $p_0(1) = 0$.</p>

  <p>Note that</p>

\[\begin{aligned}
\operatorname{Prob}(N(t+h) - N(t) = 0) &amp;= \operatorname{Prob}(N(h) - N(0) = 0) ~~\text{(by D1(2))}\\
&amp;= \operatorname{Prob}(N(h) = 0) = p_h(0) ~~\text{(by D1(3))}
\end{aligned}\]

  <p>Therefore,</p>

\[\begin{aligned}
\operatorname{Prob}(T_1 &gt; t+h) &amp;= \operatorname{Prob}(T_1 &gt; t) \operatorname{Prob}(T_1 &gt; t+h \mid T_1 &gt; t)\\
&amp;= \operatorname{Prob}(T_1 &gt; t) \operatorname{Prob}(N(t+h) - N(t) = 0)\\
&amp;= \operatorname{Prob}(T_1 &gt; t) p_h(0)\\
&amp;= \operatorname{Prob}(T_1 &gt; t) (1 - \lambda h + o(h))
\end{aligned}\]

\[\begin{aligned}
\implies \lim_{h \to 0} \frac{\operatorname{Prob}(T_1 &gt; t+h) - \operatorname{Prob}(T_1 &gt; t)}{h} &amp;= \lim_{h \to 0} \frac{\operatorname{Prob}(T_1 &gt; t)[-\lambda h+ o(h)]}{h}\\
\implies \frac{\partial \operatorname{Prob}(T_1 &gt; t)}{\partial t} &amp;= -\lambda \operatorname{Prob}(T_1 &gt; t)\\
\implies \operatorname{Prob}(T_1 &gt; t) &amp;= e^{-\lambda t}\\
\implies \operatorname{Prob}(T_1 \leq t) &amp;= 1 - e^{-\lambda t} \qquad (4)
\end{aligned}\]

  <p>That is, the time of first arrival is exponentially distributed with rate $\lambda$ and density $f(t) = \lambda e^{-\lambda t}$.</p>

  <p>Now,</p>

\[\begin{aligned}
p_t(1) &amp;= \operatorname{Prob}(N(t) = 1) = \int_0^t f(s) \operatorname{Prob}(N(t) - N(s) = 0) \mathrm{d}s ~~\text{by D1(2)}\\
&amp;= \int_0^t \lambda e^{-\lambda s} e^{-\lambda (t-s)} \mathrm{d}s\\
&amp;= \lambda t e^{-\lambda t}
\end{aligned}\]

  <p>Similarly,</p>

\[\begin{aligned}
p_t(n) &amp;= \int_{0}^t \lambda e^{-\lambda s} p_{t-s}(n-1) \mathrm{d}s
\end{aligned}\]

  <p>Assume that $p_{t-s}(n-1) = \frac{(\lambda (t-s))^{n-1}}{(n-1)!} e^{-\lambda (t-s)}$. Then we obtain the desired result using induction.</p>

  <p><span class="callout__qed">∎</span></p>
</details>

<p>Following are some properties of the Poisson process.</p>

<ol>
  <li>
    <p>Let $T_1, …, T_n$ denote the arrival times of the first $n$ events. Then the inter-arrival times $D_i = T_i - T_{i-1}$ are independent and exponentially distributed with parameter $\lambda$. We have already shown this for $D_1$ in the proof above. It applies to all $D_i$ by independence and stationarity.</p>
  </li>
  <li>
    <p>The number of events in a time interval of length $t$ has Poisson distribution with rate $\lambda t$.</p>
  </li>
  <li>
    <p>Poisson counts in an interval are a type of rare-event limit of binomial trials. See the next section for what this means.</p>
  </li>
</ol>

<h3 id="poisson-approximation-of-the-binomial">Poisson approximation of the Binomial</h3>

<p>The Binomial distribution models the number of heads $K$ in a sequence of $n$ independent coin tosses with success probability $p$:</p>

\[\text{Binomial}(k; n, p) = {n \choose k} p^k (1-p)^{n-k}.\]

<p>Now suppose the probability of a head is $p=\lambda/n$.
As the number of trials $n$ grows, $p$ goes to $0$, but the probability remains well defined at the limit.</p>

<p>To see this, expand the binomial pmf and take the limit as $n \to \infty$:</p>

\[\begin{aligned}
p(k; n, \lambda/n)
  &amp;= \frac{n(n-1)\cdots(n-k+1)}{k!} \frac{\lambda^k}{n^k}\left(1-\frac{\lambda}{n}\right)^{n-k} \\
  &amp;= \frac{\lambda^k}{k!}\left(\frac{n}{n}\frac{n-1}{n}\cdots \frac{n-k+1}{n}\right)\left(1+\frac{-\lambda}{n}\right)^n \left(1-\frac{\lambda}{n}\right)^{-k} \\
  &amp;\implies \lim_{n \to \infty} p(k; n, \lambda/n)= \frac{\lambda^k}{k!} \exp(-\lambda).
\end{aligned}\]

<p>So for large $n$, the binomial distribution with $p = \lambda/n$ is approximately Poisson with rate $\lambda$.
Letting $p = \lambda/n$ means that the probability of an individual head slowly reduces to $0$ as $n \to \infty$.</p>

<p>The figure below shows the intuition: drag $n$ to see the unit interval split into $n$ equal subintervals of length $1/n$, each with success probability $p = \lambda/n$, while the <span style="color:#5b8fd4">Binomial pmf</span> tracks the <span style="color:#e03131">Poisson$(\lambda)$ pmf</span> as the limit.</p>

<figure>
<div class="stochviz-panel" id="binom-poisson-widget">
  <div class="stochviz-controls">
    <div class="stochviz-control-group">
      <label for="binom-poisson-n">$n$</label>
      <input class="stochviz-n-slider" id="binom-poisson-n" type="range" min="5" max="500" step="5" value="10" aria-label="Number of subintervals n" />
      <span class="stochviz-meta"><span class="stochviz-n-out">10</span> subintervals, $p =$ <span class="stochviz-p-out">0.3</span></span>
    </div>
  </div>
  <div class="stochviz-interval" aria-hidden="false"></div>
  <div class="stochviz-plot" aria-hidden="false"></div>
</div>
  <figcaption>
    <b>Figure 1.</b>
    As $n$ grows, the subintervals shrink and the Binomial$(n,\lambda/n)$ pmf (blue bars) converges to the Poisson$(\lambda)$ pmf (red dots).
  </figcaption>
</figure>

<script src="/assets/js/stochviz/distributions.js"></script>

<script src="/assets/js/posts/ctmc/binomial-poisson-convergence.js"></script>

<script>
document.addEventListener('DOMContentLoaded', function () {
  var root = document.getElementById('binom-poisson-widget');
  if (root && window.createBinomialPoissonConvergenceWidget) {
    createBinomialPoissonConvergenceWidget(root, { lambda: 3, n: 10 });
  }
});
</script>

<h2 id="non-homogeneous-poisson-processes">Non-homogeneous Poisson processes</h2>

<p>In a homogeneous Poisson process, events arrive at a constant rate $\lambda$, and the number of events in an interval depends only on its length:</p>

\[N(s,t] \sim \operatorname{Poisson}\bigl(\lambda(t-s)\bigr).\]

<p>A <strong>non-homogeneous Poisson process</strong> allows the rate to vary with time <a href="#references">[1]</a>.
It keeps independent increments, but loses stationary increments: an interval during a high-rate period contains more events on average than an equally long interval during a low-rate period.</p>

<p>More formally, let $\lambda(t) \geq 0$ be a locally integrable rate function.
A counting process $N(t)$ is a non-homogeneous Poisson process with rate $\lambda(t)$ if $N(0)=0$, it has independent increments, and as $h \downarrow 0$,</p>

\[\begin{aligned}
\operatorname{Prob}\bigl(N(t,t+h]=1\bigr) &amp;= \lambda(t)h+o(h),\\
\operatorname{Prob}\bigl(N(t,t+h]&gt;1\bigr) &amp;= o(h).
\end{aligned}\]

<p>Thus $\lambda(t)h$ is approximately the probability of one event in a short interval of length $h$ beginning at $t$. Compare this to the definition of homogeneous Poisson process in the previous section.</p>

<h3 id="a-nonuniform-clock">A nonuniform clock</h3>

<p>The cleanest way to understand this process is as an ordinary Poisson process running on a nonuniform clock.
Define the <strong>cumulative intensity</strong></p>

\[\Lambda(t)=\int_0^t\lambda(r)\,\mathrm{d}r,\]

<p>and let $M(u)$ be a homogeneous Poisson process with rate $1$.
Then</p>

\[N(t)=M\bigl(\Lambda(t)\bigr)\]

<p>is a non-homogeneous Poisson process with rate $\lambda(t)$.
Physical time passes quickly on the Poisson clock when $\lambda(t)$ is large and slowly when $\lambda(t)$ is small.</p>

<p>This time change immediately gives the distribution of every increment:</p>

\[\begin{aligned}
N(s,t]
&amp;=M\bigl(\Lambda(t)\bigr)-M\bigl(\Lambda(s)\bigr)\\
&amp;\sim \operatorname{Poisson}\bigl(\Lambda(t)-\Lambda(s)\bigr)\\
&amp;=\operatorname{Poisson}\left(\int_s^t\lambda(r)\,\mathrm{d}r\right).
\end{aligned}\]

<p>Disjoint physical-time intervals map to disjoint intervals on the Poisson clock, so independent increments are preserved.
In particular,</p>

\[\mathbb{E}[N(s,t]]
=\operatorname{Var}(N(s,t])
=\int_s^t\lambda(r)\,\mathrm{d}r.\]

<p>The cumulative intensity therefore plays the same role for a non-homogeneous process that $\lambda t$ plays for a homogeneous process.</p>

<h3 id="arrival-and-waiting-times">Arrival and waiting times</h3>

<p>Let $S_n$ and $T_n$ denote the $n$th arrival times of $M$ and $N$, respectively.
If $\Lambda$ is strictly increasing, then</p>

\[T_n=\Lambda^{-1}(S_n),
\qquad
\Lambda(T_n)=S_n\sim\operatorname{Gamma}(n,1).\]

<p>When $\lambda$ vanishes on an interval, the same statement holds using the generalized inverse of $\Lambda$.
Arrival times are therefore Gamma-distributed on the cumulative-intensity clock, though generally not in physical time.</p>

<p>For the first arrival, we can derive the survival probability directly from the event that no arrivals have occurred:</p>

\[\operatorname{Prob}(T_1&gt;t)
=\operatorname{Prob}(N(t)=0)
=e^{-\Lambda(t)}.\]

<p>Differentiating gives</p>

\[f_{T_1}(t)
=\lambda(t)e^{-\Lambda(t)}.\]

<p>Equivalently, $\Lambda(T_1)\sim\operatorname{Exponential}(1)$.
More generally, if the last observed time is $s$, then</p>

\[\operatorname{Prob}(T_{\mathrm{next}}&gt;t\mid T_{\mathrm{next}}&gt;s)
=\exp\left(-\int_s^t\lambda(r)\,\mathrm{d}r\right),
\qquad t\geq s.\]

<p>Thus waiting times are exponential on the cumulative-intensity clock, but they are generally neither exponential nor identically distributed in physical time.</p>

<h3 id="prescribing-the-first-arrival-distribution">Prescribing the first-arrival distribution</h3>

<p>This is an important special case used throughout the literature on discrete flow matching and discrete diffusion.</p>

<p>Suppose that time is bounded to $[0,1]$ and we want a single event to occur with a chosen arrival-time distribution.
Let $F$ be an absolutely continuous CDF with density $f$, where $F(0)=0$ and $F(1)=1$.
We seek an intensity whose first arrival $T_1$ has CDF $F$.</p>

<p>The desired survival probability is</p>

\[S(t)=1-F(t)=e^{-\Lambda(t)}.\]

<p>Taking a derivative shows that the required intensity is precisely the hazard rate:</p>

\[\lambda(t) = \frac{f(t)}{1 - F(t)}.\]

<p>Conversely, substituting this rate into the first-arrival density recovers the chosen distribution:</p>

\[f(t) = \lambda(t)(1 - F(t)) = \lambda(t) S(t) = \lambda(t) e^{-\int_0^t \lambda(s)\,\mathrm{d}s}.\]

<p>Guaranteeing an arrival by time $1$ requires $\Lambda(t)\to\infty$ as $t\uparrow1$, so the hazard generally diverges near the endpoint.
The underlying Poisson process does not stop after this arrival and may produce further events.
To obtain a genuine one-event process, stop it at $T_1$, or equivalently use the binary process $X(t)=\mathbf{1}{T_1\leq t}$.</p>

<h3 id="references">References</h3>

<ol>
  <li>Ross, S. M. (2007). <em>Stochastic Processes</em> (2nd ed.). Wiley.</li>
</ol>]]></content><author><name>Dhruvesh Patel</name><email>dhruveshpate@umass.edu</email></author><category term="stochastic-processes" /><category term="markov-chains" /><summary type="html"><![CDATA[We go over the basics of continuous-time Markov chains, building intuition from the Poisson process up. The proofs use only basic calculus — no measure theory required.]]></summary></entry></feed>