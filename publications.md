---
layout: publications
title: Publications
permalink: /publications/
---

Full list of publications, pulled from the `publications` collection
(`_publications/*.md`). Each entry's front matter (`title`, `authors`,
`venue`, `tag`, `year`, `href`, `thumbnail`) mirrors the fields used by the
Selected Publications list on the About page — add a new file under
`_publications/` for each paper.
